function sumaOfTwoNumbers(firstNumber: number, secondNumber:number):number{
    return firstNumber + secondNumber;
}
const A:number = 5;
const B:number = 5;
const result = sumaOfTwoNumbers(A,B);
console.log(`the sum values (${A}, ${B}) is: ${result}`);
console.log('the sum values (${A}, ${B}) is: ${result}');
console.log("the sum values (${A}, ${B}) is: ${result}");